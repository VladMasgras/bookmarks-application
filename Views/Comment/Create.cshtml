@model ProiectDaw.Models.Comment
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "Comment", null, FormMethod.Post))
{
    @Html.HiddenFor(m => m.BookmarkId);

    @Html.Label("CommentBody", "Comment Body")
    <br />
    @Html.TextBox("CommentBody", null, new { @class = "form-control" })
    @Html.ValidationMessageFor(b => b.CommentBody, "", new { @class = "text-danger" })
}
